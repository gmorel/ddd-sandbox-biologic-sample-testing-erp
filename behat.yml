default:
    suites:
        consumable_engine_features:
            paths:    [ %paths.base%/test/Behavioural/consumable_engine ]
            contexts:
                - ConsumableEngineContext
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
            sessions:
                default:
                    symfony2: ~
